AOS.init(),
  window.addEventListener('load', () => {
    document.querySelector('.preloader').classList.add('preloader_hidden');
  });
const swiper = new Swiper('.swiper', {
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
    scrollbar: {},
  }),
  anchors = document.querySelectorAll('a[href^="#"]');
for (let e of anchors)
  e.addEventListener('click', function (t) {
    t.preventDefault();
    const r = e.hasAttribute('href') ? e.getAttribute('href') : 'body';
    document
      .querySelector(r)
      .scrollIntoView({ behavior: 'smooth', block: 'start' });
  });
const menuBtn = document.querySelector('.header__burger'),
  menuNav = document.querySelector('.header__nav'),
  menuUserNav = document.querySelector('.header__user-nav');
menuBtn.addEventListener('click', () => {
  menuBtn.classList.toggle('active'),
    menuNav.classList.toggle('active'),
    menuUserNav.classList.toggle('active');
}),
  menuNav.addEventListener('click', () => {
    menuNav.classList.remove('active'), menuBtn.classList.remove('active');
  });
